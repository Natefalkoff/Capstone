@model Capstone.Web.Models.SearchModel

@{
    ViewBag.Title = "Search";
}

<h2 class="body">Search for a Recipe</h2>

@using (Html.BeginForm("Search", "Search", FormMethod.Post))
{
    <div class="body">
        <div class="body">
        <h3 class="body">Check Categories For Which You Would Like To Search</h3>
            </div>
        <div class="body">
        @foreach (var kvp in Model.SearchCategories)
        {
            @*<input class="checkbox" type="checkbox" name="@kvp.Key" />
                <label class="categoryLabel" for=""*@
            <div class="category">
            @Html.HiddenFor(Model => Model.SearchCategories.Keys)
            @Html.Label(kvp.Key)
            @Html.CheckBoxFor(Model => Model.SearchCategories[kvp.Key], false)
                </div>
        }
            </div>
    </div>
        <div class="body">
            <div class="body">
            <h3> Please Enter the Tags You Would Like To Seach For.</h3> 
            <h4>If More Than 2 Words, Please Replace the Space With a Period (.)</h4>
                </div>
            <div class="padding">
                
            <h5>@Html.Label("Tag Field: ")</h5>
                
           <div id="TagSearch">@Html.TextAreaFor(Model => Model.TagSearch)
                </div>
                </div>
        </div>
            <div class="form-group,textbox"> 
                </div>
                <div class="body"> @*col-md-offset-2 col-md-10,*@ 
                    <input type="submit" value="Create" class="btn btn-default" />
            </div>

}

@{
   
    if (Model.RecipeHash != null)
    {
        foreach (var item in Model.RecipeHash)
        {
            string s = "~/App_Data/Img/" + item.ImageName.ToString();
        <div>
            <div>
                @item.Name
            </div>
            <div>
                @item.Ingredients
            </div>
            <div>
                @item.Directions
            </div>
            <div>
                
                <img src="@Url.Content("~/Img/" + item.ImageName.ToString())" alt="Recipe Image" />
            </div>
        </div>

            }
        }
    }